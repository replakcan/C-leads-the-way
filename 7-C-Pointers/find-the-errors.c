#include <stdio.h>

int main(void)
{
    // float *realPtr;
    // long *integerPtr;

    // integerPtr = realPtr;

    // int * x, y;
    // x = y;

    /*  char s[] = "this is a char array";
     char *sPtr = &s[0];
     int count;

     for (; *sPtr != '\0'; sPtr++)
     {
         printf("%c", *sPtr);
     } */

    /* short *numPtr, result;
    void *genericPtr = numPtr;

    result = *genericPtr + 7; */

    /* char *s;
    printf("%s\n", s); */

    return 0;
}