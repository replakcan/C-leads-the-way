#include <stdio.h>

void exchange(float *, float *);
int evaluate(int, int *(poly(int)));

int main(void)
{
    void exchange(float *x, float *y);

    int evaluate(int x, int *(poly(int)));

    return 0;
}